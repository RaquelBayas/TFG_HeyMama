<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApp_1" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/pink2</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/rectangle_pink</item>
        <item name="colorSecondaryVariant">@color/pink2</item>
        <item name="colorOnSecondary">@color/gray</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- No animation between activities -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>

    </style>
    <!-- AlertDialog internet connection -->
    <style name="AlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="background">@color/white</item>
        <item name="buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
        <item name="buttonBarNegativeButtonStyle">@style/NegativeButtonStyle</item>
    </style>
    <style name="PositiveButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/fonts</item>
    </style>
    <!-- Nav Bar color -->
    <style name="ThemeNavBar" parent="android:Theme">
        <item name="colorPrimary">@color/white</item>
        <item name="android:homeAsUpIndicator">@color/rectangle_orange</item>
    </style>

    <!-- Menu bar -->
    <style name="ThemeMenuBar" parent="android:Theme">
        <item name="colorPrimary">@color/white</item>
    </style>

    <style name="SwitchCompatTheme" parent="">
        <item name="colorControlActivated">@color/purple_700</item>
    </style>

    <style name="CustomBtnCerrarConsulta" >
        <item name="android:background">@drawable/rectangle_purple</item>
    </style>
    <style name="CustomForoCommentsLayout" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@drawable/dialog_comment_foro_view</item>
    </style>
    <style name="noAnimTheme" parent="android:Theme">
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>
</resources>